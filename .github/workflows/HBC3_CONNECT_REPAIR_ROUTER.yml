name: HBC3 Connect Repair Agents Router

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/HBC3_CONNECT_REPAIR_ROUTER.yml"

permissions:
  contents: write

jobs:
  connect-router:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Connect repair_agents router to FastAPI
        shell: python
        run: |
          from pathlib import Path
          import sys

          status_dir = Path("HBC_COMMAND_CENTER")
          status_dir.mkdir(exist_ok=True)
          status_file = status_dir / "HBC3_CONNECT_REPAIR_ROUTER_STATUS.md"

          required = [
              Path("backend/repair_agents/router.py"),
              Path("backend/repair_agents/diagnostic_agent.py"),
              Path("backend/repair_agents/repair_agent.py"),
              Path("backend/repair_agents/audit_agent.py"),
              Path("backend/repair_agents/schemas.py"),
              Path("backend/repair_agents/logs.py"),
              Path("backend/repair_agents/safety.py"),
          ]

          missing = [str(p) for p in required if not p.exists()]
          if missing:
              status_file.write_text(
                  "# HBC3 CONNECT ROUTER\n\n"
                  "Estado: BLOQUEO_REAL\n\n"
                  "Faltan archivos de backend/repair_agents:\n\n"
                  + "\n".join(f"- {m}" for m in missing)
                  + "\n",
                  encoding="utf-8",
              )
              raise SystemExit(f"BLOQUEO_REAL: faltan archivos: {missing}")

          preferred = [
              Path("backend/api.py"),
              Path("backend/main.py"),
              Path("main.py"),
              Path("app/main.py"),
          ]

          candidates = []

          for p in preferred:
              if p.exists():
                  candidates.append(p)

          for p in Path(".").rglob("*.py"):
              parts = set(p.parts)
              if ".git" in parts or "__pycache__" in parts or ".venv" in parts or "venv" in parts:
                  continue
              if p.as_posix().startswith("backend/repair_agents/"):
                  continue
              if p not in candidates:
                  candidates.append(p)

          fastapi_file = None
          for p in candidates:
              try:
                  text = p.read_text(encoding="utf-8")
              except UnicodeDecodeError:
                  continue

              if "FastAPI(" in text and ("app =" in text or "app=FastAPI" in text or "app = FastAPI" in text):
                  fastapi_file = p
                  break

          if fastapi_file is None:
              status_file.write_text(
                  "# HBC3 CONNECT ROUTER\n\n"
                  "Estado: BLOQUEO_REAL\n\n"
                  "No se encontró archivo principal FastAPI.\n\n"
                  "Buscado: backend/api.py, backend/main.py, main.py, app/main.py y otros .py con FastAPI().\n",
                  encoding="utf-8",
              )
              raise SystemExit("BLOQUEO_REAL: No se encontró archivo principal FastAPI.")

          text = fastapi_file.read_text(encoding="utf-8")

          if "app.include_router(repair_router" in text:
              status_file.write_text(
                  "# HBC3 CONNECT ROUTER\n\n"
                  "Estado: YA_CONECTADO\n\n"
                  f"Archivo FastAPI: `{fastapi_file.as_posix()}`\n\n"
                  "El router repair_agents ya estaba registrado.\n",
                  encoding="utf-8",
              )
              print(f"YA_CONECTADO: {fastapi_file}")
              sys.exit(0)

          import_block = (
              "\n# HBC3 repair_agents router\n"
              "try:\n"
              "    from backend.repair_agents.router import router as repair_router\n"
              "except ModuleNotFoundError:\n"
              "    from repair_agents.router import router as repair_router\n"
          )

          include_block = (
              "\n# HBC3 repair_agents router\n"
              "app.include_router(\n"
              "    repair_router,\n"
              '    prefix="/api/v1/repair",\n'
              '    tags=["repair_agents"],\n'
              ")\n"
          )

          new_text = text

          if "repair_agents.router import router as repair_router" not in new_text:
              # Insert imports after existing import block when possible.
              lines = new_text.splitlines()
              insert_at = 0
              for idx, line in enumerate(lines):
                  stripped = line.strip()
                  if stripped.startswith("import ") or stripped.startswith("from "):
                      insert_at = idx + 1
              lines.insert(insert_at, import_block.rstrip("\n"))
              new_text = "\n".join(lines) + ("\n" if text.endswith("\n") else "")

          if "app.include_router(repair_router" not in new_text:
              new_text = new_text.rstrip() + "\n" + include_block

          fastapi_file.write_text(new_text, encoding="utf-8")

          status_file.write_text(
              "# HBC3 CONNECT ROUTER\n\n"
              "Estado: CONECTADO\n\n"
              f"Archivo FastAPI: `{fastapi_file.as_posix()}`\n\n"
              "Router añadido:\n\n"
              "```python\n"
              "app.include_router(\n"
              "    repair_router,\n"
              "    prefix=\"/api/v1/repair\",\n"
              "    tags=[\"repair_agents\"],\n"
              ")\n"
              "```\n\n"
              "Estado: APTO PARA TESTING / NO VALIDADO EN PRODUCCIÓN.\n",
              encoding="utf-8",
          )

          print(f"CONECTADO: {fastapi_file}")

      - name: Validate Python syntax
        shell: bash
        run: |
          set -euo pipefail
          python -m py_compile backend/repair_agents/*.py

          FASTAPI_FILE="$(python - <<'PY'
          from pathlib import Path
          preferred = [Path("backend/api.py"), Path("backend/main.py"), Path("main.py"), Path("app/main.py")]
          candidates = preferred + [p for p in Path(".").rglob("*.py") if p not in preferred and "backend/repair_agents" not in p.as_posix()]
          for p in candidates:
              if not p.exists():
                  continue
              try:
                  t = p.read_text(encoding="utf-8")
              except UnicodeDecodeError:
                  continue
              if "FastAPI(" in t and ("app =" in t or "app=FastAPI" in t or "app = FastAPI" in t):
                  print(p.as_posix())
                  break
          PY
          )"

          if [ -n "$FASTAPI_FILE" ]; then
            python -m py_compile "$FASTAPI_FILE"
            echo "FastAPI file OK: $FASTAPI_FILE"
          else
            echo "BLOQUEO_REAL: FastAPI file not found after connection."
            exit 1
          fi

      - name: Commit connection
        shell: bash
        run: |
          set -euo pipefail

          git config user.name "hbc3-agent"
          git config user.email "hbc3-agent@users.noreply.github.com"

          git add . HBC_COMMAND_CENTER

          if git diff --cached --quiet; then
            echo "Sin cambios que confirmar."
          else
            git commit -m "Connect HBC3 repair_agents router"
            git push
          fi

      - name: Final HBC3 status
        run: |
          echo "HBC3 router conectado"
          echo "APTO PARA TESTING"
          echo "NO VALIDADO EN PRODUCCIÓN"
